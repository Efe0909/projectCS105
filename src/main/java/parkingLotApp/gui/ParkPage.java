package main.java.parkingLotApp.gui;

import javax.swing.*;

/**
 * To use a method outside
 * "{ClassName}.{MethodName}({PanelName})"
 * ========================================================================
 * e.g.: ParkPage.vehicleSelectionButton(homepage);
 */
public class ParkPage {
    public static void vehicleSelectionButton(JPanel panel) {
        JButton button = new JButton("Vehicle Selection");
        // bla bla

        panel.add(button);
    }




}
